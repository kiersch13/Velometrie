// This file runs once before every test file.
// It bootstraps Angular's test utilities and zone.js for Jest.
import { setupZoneTestEnv } from 'jest-preset-angular/setup-env/zone';

setupZoneTestEnv();
