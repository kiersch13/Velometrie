// Production environment — swapped in during `ng build --configuration production`
// Replace RAILWAY_BACKEND_URL with your actual Railway backend service URL
// e.g. https://bikewear-backend-production.up.railway.app
export const environment = {
  production: true,
  apiBaseUrl: 'RAILWAY_BACKEND_URL',
};
