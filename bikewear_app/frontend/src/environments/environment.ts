// Development environment — used when running `ng serve` or `npm run build`
export const environment = {
  production: false,
  // Backend URL on your local machine
  apiBaseUrl: 'http://localhost:5059',
};
